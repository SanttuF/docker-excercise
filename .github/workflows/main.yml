name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.98ddc61a-95fb-4a1c-8b51-5b9c2373a066}}
          heroku_app_name: web-service excercise
          heroku_email: santtu.f@gmail.com